// if (!numbers || numbers.length === 0) return null;
// let ans = Infinity;

// for (let i = 0; i < numbers.length; i++) {
//   for (let j = 0; j < numbers.length; j++) {
//     if (j !== i) {
//       let interval = Math.abs(numbers[i] - numbers[j]);
//       if (ans > interval) ans = interval;
//     }
//   }
// }

// return ans;
